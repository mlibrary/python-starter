services:
  project_1:
    build: 
      context: ../../.
      target: builder
      dockerfile: projects/project_1/Dockerfile
      args:
        DEV: true
    volumes:
      - .:/project_1
      - .:/tmp:rm
    tty: true
    stdin_open: true
volumes:
  project_1:
  tmp:

